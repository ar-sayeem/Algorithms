#include<stdio.h>

// Global variable.
int a, b, temp=0;

int swap()
{
// Swapping
    temp = a;
    a = b;
    b = temp;
    printf("After Swapping:\n a = %d\n b = %d", a, b );
}
int main()
{
    scanf("%d %d", &a, &b);     // Input
    swap();                     // Function Call
}

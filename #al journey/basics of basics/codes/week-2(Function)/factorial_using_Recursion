#include<stdio.h>
int factRec(unsigned int i)
{
    if( i<=1 ){
        return 1;}
    return i*factRec(i-1);
}

int main()
{
    int i;
    scanf("%d", &i);
    printf("Factorial of %d = %d", i, factRec(i));
    return 0;
}
